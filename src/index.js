import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));   // help to catch the root component 

root.render(<App />); // render the content to the web page